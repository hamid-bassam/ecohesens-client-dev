// import { useQuery } from '@tanstack/react-query';
// import { fetchBox } from '../services/boxService';

export function useBoxDetails(id: string) {
  // return useQuery({ queryKey: ['box', id], queryFn: () => fetchBox(id) });
}